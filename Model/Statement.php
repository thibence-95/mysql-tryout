<?php
declare(strict_types = 1);

class Statement
{
    /*public function insert(object $object) : string {
        $statement = 'INSERT INTO student(first_name, last_name, username, linkedin, email, preferred_language, avatar, video, quote, quote_author, created_at) 
                    VALUES ('. $object->getFirstName(). ','.$object->getLastName(). ','. $object->getUsername(). ','. $object->getLinkedin(). ','
            . $object->getEmail(). ','. $object->getLang(). ','. $object->getAvatar(). ','
            . $object->getVideo(). ','. $object->getQuote(). ','. $object->getQuoteAuth().')';

        var_dump($statement);
        return $statement;
    }*/
}